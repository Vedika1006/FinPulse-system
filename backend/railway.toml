[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Do not set startCommand here — it overrides the Dockerfile CMD and Railway
# may not expand $PORT without a shell. start.sh handles PORT correctly.
