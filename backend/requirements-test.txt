# CI/test dependencies — everything the app needs to import and run its test
# suite, minus the slow ML packages (prophet, cmdstanpy) which are lazily
# imported at runtime and not required for tests (analytics_service falls
# back to a rule-based forecast when prophet isn't installed).

fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.2.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
python-dotenv>=1.0.0
email-validator>=2.0.0
groq
pandas==2.2.2
faiss-cpu==1.8.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
apscheduler>=3.10
python-dateutil>=2.8.2

# Testing
pytest>=7.0
httpx>=0.24
