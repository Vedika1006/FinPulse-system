nixPkgs = ["python311"]
aptPkgs = ["libstdc++6", "zlib1g", "libgomp1"]

[phases.install]
cmds = ["pip install -r requirements.txt --no-cache-dir"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"